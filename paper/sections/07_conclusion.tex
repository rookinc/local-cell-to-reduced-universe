\section{Conclusion}

Project 22 generated the local answer cell.  This paper derives the reduced universe from that cell.

The mechanism is simple:
\[
    \text{one cell}
    \longrightarrow
    \text{two readouts}
    \longrightarrow
    16\ \text{visible candidates}
    \longrightarrow
    \text{hidden diagonal}.
\]

The selected set \([1,6,8,15]\) is no longer an unexplained choice inside a copied reduced universe.  It is the visible diagonal of two readouts of the same generated local cell.

The remaining open problem is to derive the local cell itself from native G60 provenance.
